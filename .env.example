# Copy to `.env` and fill in. Do not commit real keys.
# Google AI Studio / Gemini — used by google-generativeai in main.py
GEMINI_API_KEY=
# Default in code is gemini-2.5-flash; override if your project uses another ID
GEMINI_MODEL=gemini-2.5-flash
